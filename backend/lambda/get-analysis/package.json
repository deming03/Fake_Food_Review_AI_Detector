{
  "name": "get-analysis-lambda",
  "version": "1.0.0",
  "description": "Lambda function to retrieve analysis results",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "zip -r function.zip . && aws lambda update-function-code --function-name get-analysis --zip-file fileb://function.zip"
  },
  "keywords": [
    "aws",
    "lambda",
    "analysis",
    "results"
  ],
  "author": "Fake Food Detector Team",
  "license": "MIT",
  "dependencies": {
    "aws-sdk": "^2.1691.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
