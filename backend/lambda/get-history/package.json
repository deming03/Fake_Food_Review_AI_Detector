{
  "name": "get-history-lambda",
  "version": "1.0.0",
  "description": "Lambda function to retrieve analysis history",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "zip -r function.zip . && aws lambda update-function-code --function-name get-history --zip-file fileb://function.zip"
  },
  "keywords": [
    "aws",
    "lambda",
    "history",
    "analysis"
  ],
  "author": "Fake Food Detector Team",
  "license": "MIT",
  "dependencies": {
    "aws-sdk": "^2.1691.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
